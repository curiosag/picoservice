
Closures und Namensraum für Werte? Lambdas funktionieren so, daß richtig benannte werte bis zu lambda L durchgereicht werden,
L muß dann das als formalen parameter haben

mapping formale/aktuelle parameter

recovery:
auf per-value basis unterscheiden, ob propagation schon vlollständig passiert ist (valueProcessed) oder nicht


TODOs
String ids derived from runId+computationpath globally, mapping to numeric id for debugging/tracing purposes
